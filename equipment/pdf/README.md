# equipment/pdf/

Scripts de génération PDF. Un script = une tâche.

À construire avec Build Queue #1 (devis et factures).
Input : données structurées (JSON ou Markdown). Output : PDF dans `live/documents/exports-pdf/`.
