# PARTIE 3 — Branding, PAID Watermark, Social Standalone, Routines

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ajouter branding centralisé, filigrane PAYÉ, mode autonome pour les posts sociaux, et 3 routines planifiées à l'EA HHSA Agency.

**Architecture:** `brand.py` est un module centralisé importé par les 3 scripts PDF. `generate_pdf_facture.py` reçoit un flag `--paid` qui déclenche le filigrane diagonal rouge. `contenu-social` est mis à niveau pour fonctionner sans fichier findings. 3 routines CronCreate tournent dans le cloud indépendamment de Claude Code.

**Tech Stack:** Python 3.12 · fpdf2 2.5+ · pytest · Claude Code `schedule` skill (CronCreate)

---

## File Map

| Fichier | Action | Responsabilité |
|---------|--------|----------------|
| `equipment/brand.py` | CREATE | BRAND dict, safe(), apply_header(), apply_footer(), apply_paid_watermark() |
| `tests/test_brand.py` | CREATE | Tests unitaires brand.py |
| `tests/test_pdf_integration.py` | CREATE | Tests intégration — génération PDF réel |
| `equipment/generate_pdf_facture.py` | MODIFY | Import brand.py, FacturePDF utilise brand, flag --paid |
| `equipment/generate_pdf_devis.py` | MODIFY | Import brand.py, DevisPDF utilise brand |
| `equipment/generate_pdf_social.py` | MODIFY | Import brand.py, SocialPDF utilise brand |
| `.claude/skills/devis-et-factures/SKILL.md` | CREATE | Skill complet pour devis et factures |
| `.claude/skills/contenu-social/SKILL.md` | MODIFY | Ajouter Mode B autonome (sans findings) |
| `routines/pipeline-recap.md` | CREATE | Définition routine lundi 08h30 |
| `routines/veille-contenu.md` | CREATE | Définition routine mercredi 10h00 |
| `routines/bilan-semaine.md` | CREATE | Définition routine vendredi 17h00 |

---

### Task 1: brand.py — Module centralisé

**Files:**
- Create: `equipment/brand.py`
- Create: `tests/test_brand.py`

- [ ] **Step 1: Créer le dossier tests/**

```bash
mkdir tests
```

- [ ] **Step 2: Écrire tests/test_brand.py**

```python
import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).parent.parent))

from equipment.brand import BRAND, safe


def test_brand_has_required_keys():
    for key in ("name", "gold", "dark", "body", "light", "logo"):
        assert key in BRAND, f"BRAND missing key: {key}"


def test_brand_colors_are_rgb_tuples():
    for key in ("gold", "dark", "body", "light"):
        t = BRAND[key]
        assert isinstance(t, tuple) and len(t) == 3
        assert all(0 <= v <= 255 for v in t)


def test_brand_gold_is_correct():
    assert BRAND["gold"] == (212, 160, 23)


def test_brand_dark_is_correct():
    assert BRAND["dark"] == (26, 26, 26)


def test_safe_replaces_em_dash():
    assert "--" in safe("avant — apr\xe8s")


def test_safe_replaces_curly_quotes():
    result = safe("‘hello’")
    assert "'" in result


def test_safe_encodes_to_latin1():
    result = safe("caf\xe9")
    assert isinstance(result, str)


def test_safe_handles_plain_ascii():
    assert safe("hello world") == "hello world"
```

- [ ] **Step 3: Vérifier que les tests échouent**

```bash
cd "C:\COHORT_2026\HHSA ASSISTANT EXECUTIF"
python -m pytest tests/test_brand.py -v
```

Expected: `ModuleNotFoundError` — `equipment.brand` n'existe pas encore.

- [ ] **Step 4: Créer equipment/brand.py**

```python
#!/usr/bin/env python3
"""brand.py — HHSA Agency centralized branding. Imported by all PDF scripts."""

from pathlib import Path

BRAND = {
    "name":  "HHSA Agency",
    "gold":  (212, 160, 23),
    "dark":  (26, 26, 26),
    "body":  (51, 53, 71),
    "light": (100, 116, 139),
    "logo":  str(Path(__file__).parent / "assets" / "LOGO.png"),
}

_REPLACEMENTS = {
    "—": "--", "–": "-", "‒": "-",
    "‘": "'",  "’": "'", "‚": ",",
    "“": '"',  "”": '"', "„": '"',
    "…": "...", "·": "-", "•": "-",
    " ": " ",  "​": "",
    "→": "->", "←": "<-", "×": "x",
}


def safe(text: str) -> str:
    for src, dst in _REPLACEMENTS.items():
        text = text.replace(src, dst)
    return text.encode("latin-1", errors="replace").decode("latin-1")


def apply_header(pdf) -> None:
    from fpdf import XPos, YPos
    pdf.set_fill_color(*BRAND["dark"])
    pdf.rect(0, 0, 210, 20, "F")
    logo = BRAND["logo"]
    if Path(logo).exists():
        try:
            pdf.image(logo, x=85, y=3, h=14)
        except Exception:
            _header_text_fallback(pdf)
    else:
        _header_text_fallback(pdf)
    pdf.set_text_color(0, 0, 0)
    pdf.set_y(22)


def _header_text_fallback(pdf) -> None:
    from fpdf import XPos, YPos
    pdf.set_font("Helvetica", "B", 10)
    pdf.set_text_color(255, 255, 255)
    pdf.set_y(6)
    pdf.cell(0, 8, "HHSA AGENCY", align="C",
             new_x=XPos.LMARGIN, new_y=YPos.NEXT)


def apply_footer(pdf) -> None:
    pdf.set_y(-14)
    pdf.set_font("Helvetica", "I", 7.5)
    pdf.set_text_color(*BRAND["light"])
    pdf.cell(
        0, 10,
        f"HHSA Agency  --  Document confidentiel  --  Page {pdf.page_no()}/{{nb}}",
        align="C",
    )


def apply_paid_watermark(pdf, text: str = "PAYE") -> None:
    current_y = pdf.get_y()
    pdf.set_font("Helvetica", "B", 72)
    pdf.set_text_color(220, 53, 69)
    pdf.set_alpha(0.25)
    with pdf.rotation(45, x=105, y=148.5):
        pdf.text(x=25, y=170, txt=safe(text))
    pdf.set_alpha(1.0)
    pdf.set_text_color(0, 0, 0)
    pdf.set_y(current_y)
```

- [ ] **Step 5: Lancer les tests**

```bash
python -m pytest tests/test_brand.py -v
```

Expected: 8 tests PASS.

- [ ] **Step 6: Commit**

```bash
git add equipment/brand.py tests/test_brand.py
git commit -m "feat: add brand.py — centralized HHSA Agency branding module"
```

---

### Task 2: generate_pdf_facture.py — Branding + flag --paid

**Files:**
- Modify: `equipment/generate_pdf_facture.py`
- Create: `tests/test_pdf_integration.py`

- [ ] **Step 1: Écrire le test d'intégration**

Créer `tests/test_pdf_integration.py` :

```python
import sys
import subprocess
from pathlib import Path
sys.path.insert(0, str(Path(__file__).parent.parent))

FACTURE_MD = "live/documents/factures/FAC-20260520-001.md"


def test_generate_facture_standard(tmp_path):
    from equipment.generate_pdf_facture import generate_facture
    out = tmp_path / "facture_test.pdf"
    result = generate_facture(FACTURE_MD, str(out), paid=False)
    assert Path(result).exists()
    assert Path(result).stat().st_size > 1000


def test_generate_facture_paid(tmp_path):
    from equipment.generate_pdf_facture import generate_facture
    out = tmp_path / "facture_paid_test.pdf"
    result = generate_facture(FACTURE_MD, str(out), paid=True)
    assert Path(result).exists()
    assert Path(result).stat().st_size > 1000


def test_cli_paid_flag(tmp_path):
    out = tmp_path / "facture_cli.pdf"
    result = subprocess.run(
        [sys.executable, "equipment/generate_pdf_facture.py",
         "--input", FACTURE_MD,
         "--output", str(out),
         "--paid"],
        capture_output=True, text=True
    )
    assert result.returncode == 0
    assert out.exists()
```

- [ ] **Step 2: Vérifier que les tests échouent**

```bash
python -m pytest tests/test_pdf_integration.py::test_generate_facture_standard tests/test_pdf_integration.py::test_generate_facture_paid -v
```

Expected: `TypeError` — `generate_facture()` ne prend pas de paramètre `paid` encore.

- [ ] **Step 3: Modifier generate_pdf_facture.py**

Remplacer les **6 blocs** suivants dans `equipment/generate_pdf_facture.py` :

**A) Ajouter les imports brand en haut du fichier (après les imports stdlib) :**

```python
import sys
import re
import argparse
from datetime import datetime
from pathlib import Path

# Brand centralisé
sys.path.insert(0, str(Path(__file__).parent))
from brand import safe, BRAND, apply_header, apply_footer, apply_paid_watermark
```

**B) Supprimer les constantes de couleur inline** (lignes `DARK = ...`, `LIGHT_TEXT = ...`, etc.) et les remplacer par :

```python
DARK       = BRAND["dark"]
LIGHT_TEXT = BRAND["light"]
BODY_TEXT  = BRAND["body"]
ACCENT     = (16, 185, 129)
PAGE_BG    = (248, 250, 252)
```

**C) Supprimer la fonction `safe()` inline** (elle vient maintenant de brand.py).

**D) Modifier la classe `FacturePDF` — header, footer, paid :**

```python
class FacturePDF(FPDF):
    paid: bool = False

    def header(self):
        apply_header(self)
        if self.paid:
            apply_paid_watermark(self)

    def footer(self):
        apply_footer(self)
```

**E) Modifier la signature de `generate_facture` pour accepter `paid` :**

```python
def generate_facture(input_path: str, output_path: str, paid: bool = False) -> str:
    FPDF, XPos, YPos = ensure_fpdf()
    ...
    pdf = FacturePDF()
    pdf.paid = paid
    ...
```

**F) Modifier le bloc `__main__` pour ajouter `--paid` :**

```python
if __name__ == "__main__":
    parser = argparse.ArgumentParser(
        description="Génère un PDF de facture HHSA depuis un fichier Markdown."
    )
    parser.add_argument("--input",  required=True, help="Chemin vers le fichier facture .md")
    parser.add_argument("--output", required=True, help="Chemin de sortie du PDF")
    parser.add_argument("--paid",   action="store_true", help="Ajouter le filigrane PAYÉ")
    args = parser.parse_args()

    path = generate_facture(args.input, args.output, paid=args.paid)
    print(f"Succès : {path}")
```

- [ ] **Step 4: Lancer les tests**

```bash
python -m pytest tests/test_pdf_integration.py -v
```

Expected: 3 tests PASS.

- [ ] **Step 5: Test manuel visuel**

```bash
python equipment/generate_pdf_facture.py \
  --input live/documents/factures/FAC-20260520-001.md \
  --output live/documents/exports-pdf/FAC-20260520-001-PAYE.pdf \
  --paid
```

Ouvrir `live/documents/exports-pdf/FAC-20260520-001-PAYE.pdf` et vérifier :
- Logo HHSA gold visible en haut
- Filigrane "PAYE" diagonal rouge semi-transparent visible sur la page

- [ ] **Step 6: Commit**

```bash
git add equipment/generate_pdf_facture.py tests/test_pdf_integration.py
git commit -m "feat: add brand.py integration and --paid watermark to generate_pdf_facture"
```

---

### Task 3: generate_pdf_devis.py — Branding

**Files:**
- Modify: `equipment/generate_pdf_devis.py`

- [ ] **Step 1: Ajouter le test dans test_pdf_integration.py**

Ajouter à la fin de `tests/test_pdf_integration.py` :

```python
DEVIS_MD = "templates/devis/devis-template.md"


def test_generate_devis_standard(tmp_path):
    from equipment.generate_pdf_devis import generate_devis
    out = tmp_path / "devis_test.pdf"
    result = generate_devis(DEVIS_MD, str(out))
    assert Path(result).exists()
    assert Path(result).stat().st_size > 1000
```

- [ ] **Step 2: Vérifier que le test passe déjà (branding non encore branché)**

```bash
python -m pytest tests/test_pdf_integration.py::test_generate_devis_standard -v
```

Expected: PASS (le script génère déjà un PDF, on vérifie juste l'existence).

- [ ] **Step 3: Modifier generate_pdf_devis.py**

Même pattern que Task 2, sans `--paid` :

**A) Remplacer les imports en haut :**

```python
import sys
import re
import argparse
from datetime import datetime
from pathlib import Path

sys.path.insert(0, str(Path(__file__).parent))
from brand import safe, BRAND, apply_header, apply_footer
```

**B) Supprimer les constantes inline, remplacer par :**

```python
DARK       = BRAND["dark"]
LIGHT_TEXT = BRAND["light"]
BODY_TEXT  = BRAND["body"]
ACCENT     = (59, 130, 246)
PAGE_BG    = (248, 250, 252)
```

**C) Supprimer la fonction `safe()` inline.**

**D) Modifier la classe `DevisPDF` :**

```python
class DevisPDF(FPDF):
    def header(self):
        apply_header(self)

    def footer(self):
        apply_footer(self)
```

- [ ] **Step 4: Lancer les tests**

```bash
python -m pytest tests/test_pdf_integration.py::test_generate_devis_standard -v
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add equipment/generate_pdf_devis.py tests/test_pdf_integration.py
git commit -m "feat: apply brand.py branding to generate_pdf_devis"
```

---

### Task 4: generate_pdf_social.py — Branding

**Files:**
- Modify: `equipment/generate_pdf_social.py`

- [ ] **Step 1: Ajouter le test dans test_pdf_integration.py**

Ajouter à la fin de `tests/test_pdf_integration.py` :

```python
import json

def test_generate_social_pdf(tmp_path):
    from equipment.generate_pdf_social import generate_social_pdf

    posts_data = {
        "brand": "Soukwany",
        "topic": "E-commerce Maroc",
        "date": "20 Mai 2026",
        "posts": {
            "linkedin":  {"text": "Test LinkedIn post content here.", "hashtags": ["#Ecommerce"]},
            "facebook":  {"text": "Test Facebook post content here.", "hashtags": ["#Maroc"]},
            "instagram": {"text": "Test Instagram post here.", "hashtags": ["#Shop"]},
        }
    }
    input_json = tmp_path / "posts_test.json"
    input_json.write_text(json.dumps(posts_data), encoding="utf-8")
    out = tmp_path / "posts_test.pdf"

    result = generate_social_pdf(str(input_json), str(out))
    assert Path(result).exists()
    assert Path(result).stat().st_size > 1000
```

- [ ] **Step 2: Vérifier que le test passe (script existant)**

```bash
python -m pytest tests/test_pdf_integration.py::test_generate_social_pdf -v
```

Expected: PASS.

- [ ] **Step 3: Modifier generate_pdf_social.py**

**A) Remplacer les imports en haut :**

```python
import json
import sys
import argparse
from datetime import datetime
from pathlib import Path

sys.path.insert(0, str(Path(__file__).parent))
from brand import safe, BRAND, apply_header, apply_footer
```

**B) Supprimer les constantes `DARK` et `LIGHT_TEXT` inline, remplacer par :**

```python
DARK       = BRAND["dark"]
LIGHT_TEXT = BRAND["light"]
BODY_TEXT  = BRAND["body"]
```

**C) Supprimer la fonction `safe()` inline.**

**D) Modifier la classe `SocialPDF` :**

```python
class SocialPDF(FPDF):
    def header(self):
        apply_header(self)

    def footer(self):
        apply_footer(self)
```

- [ ] **Step 4: Lancer tous les tests**

```bash
python -m pytest tests/ -v
```

Expected: tous PASS.

- [ ] **Step 5: Commit**

```bash
git add equipment/generate_pdf_social.py tests/test_pdf_integration.py
git commit -m "feat: apply brand.py branding to generate_pdf_social"
```

---

### Task 5: Skill devis-et-factures

**Files:**
- Create: `.claude/skills/devis-et-factures/SKILL.md`

- [ ] **Step 1: Créer le dossier**

```bash
mkdir .claude/skills/devis-et-factures
```

- [ ] **Step 2: Créer .claude/skills/devis-et-factures/SKILL.md**

```markdown
---
name: devis-et-factures
description: Génère un devis ou une facture PDF brandé HHSA Agency depuis un template Markdown. Déclencher quand l'utilisateur dit "génère un devis pour", "crée une facture pour", "prépare la facture", ou "marque la facture comme payée".
allowed-tools: Bash, Read, Write
---

Génère un devis ou une facture PDF brandé. Lit le template → remplit les données → valide → génère le PDF.

## Inputs Requis

| Input | Obligatoire |
|-------|-------------|
| Type (devis ou facture) | Oui |
| Nom du client | Oui |
| Nom + email du contact | Oui |
| Liste des prestations + montants | Oui |
| Conditions de paiement | Oui |
| Statut payé (oui/non) | Non — défaut : non |
| N° devis de référence | Si facture |

Si un input financier est manquant → **STOP. Lister ce qui manque.**

## Séquence

**Étape 1 — Confirmer les inputs**
Lister les informations manquantes. Ne pas commencer sans données financières complètes.

**Étape 2 — Lire le template**
- Devis → `templates/devis/devis-template.md`
- Facture → `templates/factures/facture-template.md`

**Étape 3 — Générer le contenu Markdown**
Attribuer le numéro :
- Devis : `DEV-[YYYYMMDD]-[NNN]` (NNN commence à 001)
- Facture : `FAC-[YYYYMMDD]-[NNN]`

**Étape 4 — Présenter pour validation**
Afficher le document complet. Attendre approbation ou corrections.

**Étape 5 — Sauvegarder le fichier MD source**
- Devis : `live/documents/devis/DEV-[YYYYMMDD]-[NNN].md`
- Facture : `live/documents/factures/FAC-[YYYYMMDD]-[NNN].md`

**Étape 6 — Générer le PDF**

Devis standard :
```bash
python equipment/generate_pdf_devis.py \
  --input live/documents/devis/DEV-YYYYMMDD-NNN.md \
  --output live/documents/exports-pdf/DEV-YYYYMMDD-NNN.pdf
```

Facture standard :
```bash
python equipment/generate_pdf_facture.py \
  --input live/documents/factures/FAC-YYYYMMDD-NNN.md \
  --output live/documents/exports-pdf/FAC-YYYYMMDD-NNN.pdf
```

Facture payée (filigrane PAYÉ) :
```bash
python equipment/generate_pdf_facture.py \
  --input live/documents/factures/FAC-YYYYMMDD-NNN.md \
  --output live/documents/exports-pdf/FAC-YYYYMMDD-NNN-PAYE.pdf \
  --paid
```

**Étape 7 — Copier dans le dossier client**
Copier le PDF dans `clients/[nom-client]/devis/` ou `clients/[nom-client]/factures/`.
Si le dossier n'existe pas → créer `clients/[nom-client]/` avec sous-dossiers `devis/` et `factures/`.

**Étape 8 — Reporter**
```
[Type] créé : [N° document]
PDF    : live/documents/exports-pdf/[N°].pdf
Client : clients/[nom-client]/[type]/[N°].pdf
Statut : [Standard | PAYÉ — filigrane appliqué]
```

## Gestion des Erreurs

| Situation | Action |
|-----------|--------|
| Montants manquants | STOP. Demander les montants exacts avant de continuer. |
| Script PDF échoue | Fournir le Markdown source — utilisable pour copier-coller. |
| Dossier client inexistant | Créer automatiquement. |
| Demande d'envoi direct | Refuser. "Je prépare le PDF. Tu l'envoies toi-même après vérification." |
```

- [ ] **Step 3: Enregistrer dans CLAUDE.md**

Dans `CLAUDE.md`, section **Skills Actifs** (le tableau avec colonnes Skill / Déclencheur / Ce que ça fait), ajouter cette ligne à la fin du tableau :

```markdown
| `devis-et-factures` | "génère un devis pour [client]" / "crée une facture pour [client]" / "marque la facture [N°] comme payée" | Template → MD → PDF brandé → copie dans clients/ |
```

- [ ] **Step 4: Commit**

```bash
git add .claude/skills/devis-et-factures/SKILL.md CLAUDE.md
git commit -m "feat: add devis-et-factures skill with --paid support"
```

---

### Task 6: contenu-social — Mode B autonome

**Files:**
- Modify: `.claude/skills/contenu-social/SKILL.md`

- [ ] **Step 1: Remplacer le contenu de .claude/skills/contenu-social/SKILL.md**

```markdown
---
name: contenu-social
description: Transforme un sujet OU les findings de recherche en 3 posts sociaux (LinkedIn, Facebook, Instagram) en français. Génère un JSON et un PDF dans live/research/. Mode B (autonome) si sujet fourni. Mode A (findings) si aucun sujet.
allowed-tools: Bash, Read, Write
---

Génère 3 posts sociaux depuis un sujet direct (Mode B) ou le fichier findings le plus récent (Mode A).
Marque par défaut si non spécifiée : **HHSA Agency**.

## Inputs

| Input | Obligatoire | Défaut |
|-------|-------------|--------|
| Sujet / thème | Non* | — |
| Nom de marque | Non | HHSA Agency |

*Si aucun sujet fourni → Mode A (findings). Si sujet fourni → Mode B (autonome).

## Mode B — Autonome (sujet fourni directement)

**Déclencheurs :** "génère les posts pour [marque] sur [sujet]", "crée des posts sur [sujet]"

**Étape 1 — Collecter les inputs**
Si le sujet n'est pas dans le déclencheur → demander : "Sur quel sujet ?"
Si la marque n'est pas précisée → utiliser HHSA Agency.

**Étape 2 — Générer les 3 posts en français**

| Plateforme | Ton | Longueur | Hashtags | Structure |
|------------|-----|----------|----------|-----------|
| LinkedIn | Professionnel, expert, pédagogique | 150-200 mots | 3 ciblés secteur | Accroche → insight → développement → CTA professionnel |
| Facebook | Chaleureux, engageant, accessible | 80-120 mots | 2-3 | Accroche conversationnelle → point clé → question ou CTA |
| Instagram | Percutant, hook fort ligne 1 | 50-80 mots | 8-10 | Hook → 2-3 lignes clés → CTA court → hashtags |

Règles : tous en français · une seule idée centrale par post · CTA adapté à la plateforme.

**Étape 3 — Sauvegarder le JSON**
Créer `live/research/posts-YYYYMMDD.json` :

```json
{
  "brand": "[Nom de marque]",
  "topic": "[Sujet]",
  "date": "[JJ Mois Année]",
  "posts": {
    "linkedin":  { "text": "...", "hashtags": ["#Tag1", "#Tag2", "#Tag3"] },
    "facebook":  { "text": "...", "hashtags": ["#Tag1", "#Tag2"] },
    "instagram": { "text": "...", "hashtags": ["#Tag1", "..."] }
  }
}
```

**Étape 4 — Générer le PDF**
```bash
python equipment/generate_pdf_social.py \
  --input live/research/posts-YYYYMMDD.json \
  --output live/research/posts-YYYYMMDD.pdf
```

**Étape 5 — Afficher les posts pour relecture**

**Étape 6 — Reporter**
```
3 posts générés pour [Marque] — Sujet : [Sujet]
JSON : live/research/posts-YYYYMMDD.json
PDF  : live/research/posts-YYYYMMDD.pdf
```

## Mode A — Findings (aucun sujet fourni)

**Déclencheurs :** "crée les posts sociaux" (sans sujet précisé)

**Étape 1 — Auto-détecter le fichier findings**
```bash
ls -t live/research/findings-*.json | head -1
```
Si aucun fichier → STOP : *"Aucune recherche trouvée. Lance d'abord : 'fais une recherche sur [sujet]'."*

**Étape 2 — Lire le fichier**
Extraire : `topic`, `executive_summary`, `findings`.

**Étapes 3-6** → identiques au Mode B (même structure JSON, même PDF, même relecture).

## Gestion des erreurs

| Situation | Action |
|-----------|--------|
| Mode A : aucun findings | STOP. "Lance d'abord : 'fais une recherche sur [sujet]'." |
| Script PDF échoue | Afficher le JSON brut + signaler l'erreur |
| Ton jugé incorrect | Re-rédiger le post entier plutôt que patcher |
```

- [ ] **Step 2: Commit**

```bash
git add .claude/skills/contenu-social/SKILL.md
git commit -m "feat: upgrade contenu-social to Mode B — standalone without findings dependency"
```

---

### Task 7: 3 Routines planifiées (CronCreate)

**Files:**
- Create: `routines/pipeline-recap.md`
- Create: `routines/veille-contenu.md`
- Create: `routines/bilan-semaine.md`

- [ ] **Step 1: Créer le dossier routines/**

```bash
mkdir routines
```

- [ ] **Step 2: Créer routines/pipeline-recap.md**

```markdown
# Routine : Pipeline Recap

**Cron (UTC) :** `30 7 * * 1`
**Heure locale :** Lundi 08h30 Casablanca (UTC+1)
**Description :** Résumé hebdomadaire du pipeline commercial depuis Google Sheets.

## Prompt

Lance le skill recap-pipeline. Génère le résumé complet du CRM :
- Leads par étape (Prospect / Qualifié / Proposition / Négociation / Gagné / Perdu)
- Valeur totale du pipeline actif
- Top 3 leads les plus chauds avec action recommandée
- Follow-ups dus cette semaine (Last Contact > 7 jours)

Présente le résumé de façon claire et actionnable pour démarrer la semaine.
```

- [ ] **Step 3: Créer routines/veille-contenu.md**

```markdown
# Routine : Veille + Contenu Social

**Cron (UTC) :** `0 9 * * 3`
**Heure locale :** Mercredi 10h00 Casablanca (UTC+1)
**Description :** Recherche tendances agentiques → génère 3 posts sociaux prêts à publier.

## Prompt

Étape 1 : Lance le skill recherche-tendances sur le sujet "workflows agentiques PME MENA 2026".
Étape 2 : Une fois les findings générés, lance le skill contenu-social en Mode A.
Résultat attendu : 3 posts LinkedIn/Facebook/Instagram + PDF dans live/research/.
Reporter le chemin du PDF généré.
```

- [ ] **Step 4: Créer routines/bilan-semaine.md**

```markdown
# Routine : Bilan Semaine

**Cron (UTC) :** `0 16 * * 5`
**Heure locale :** Vendredi 17h00 Casablanca (UTC+1)
**Description :** Résumé de fin de semaine + priorités semaine suivante en draft Gmail.

## Prompt

Génère un bilan de semaine pour HHSA Agency :
1. Résumé des tâches accomplies cette semaine (consulter git log --oneline --since="7 days ago")
2. Liste des points ouverts restants
3. Top 3 priorités pour la semaine prochaine
4. Une action concrète à faire lundi matin

Crée un brouillon Gmail à ezzouhir2122@gmail.com avec l'objet :
"Bilan semaine [DD/MM] — HHSA Agency"
et le bilan complet dans le corps.
Note : Ne pas envoyer — brouillon seulement.
```

- [ ] **Step 5: Enregistrer les routines via le skill schedule**

Pour chaque routine, utiliser le skill `schedule` pour créer le CronCreate :

```
/schedule pipeline-recap — Lundi 08h30 Casablanca — Lance recap-pipeline
cron: 30 7 * * 1
prompt: [contenu du fichier routines/pipeline-recap.md]

/schedule veille-contenu — Mercredi 10h00 Casablanca — Recherche + posts sociaux
cron: 0 9 * * 3
prompt: [contenu du fichier routines/veille-contenu.md]

/schedule bilan-semaine — Vendredi 17h00 Casablanca — Bilan + draft Gmail
cron: 0 16 * * 5
prompt: [contenu du fichier routines/bilan-semaine.md]
```

- [ ] **Step 6: Commit final**

```bash
git add routines/
git commit -m "feat: add 3 scheduled routines — pipeline recap, veille+contenu, bilan semaine"
```

---

## Tests de validation finale

```bash
# Tous les tests unitaires + intégration
python -m pytest tests/ -v

# Facture standard
python equipment/generate_pdf_facture.py \
  --input live/documents/factures/FAC-20260520-001.md \
  --output live/documents/exports-pdf/FAC-20260520-001.pdf

# Facture payée
python equipment/generate_pdf_facture.py \
  --input live/documents/factures/FAC-20260520-001.md \
  --output live/documents/exports-pdf/FAC-20260520-001-PAYE.pdf \
  --paid
```

Résultats attendus :
- Tous les tests PASS
- `FAC-20260520-001.pdf` : logo HHSA gold, sans filigrane
- `FAC-20260520-001-PAYE.pdf` : logo HHSA gold + filigrane "PAYE" diagonal rouge

---

*Plan créé : 2026-05-20 · Spec : docs/superpowers/specs/2026-05-20-partie3-design.md*
